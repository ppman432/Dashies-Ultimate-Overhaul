wargoal_types = {
    ceasefire_resume_war = {
        # Script-only wargoal used by Ceasefire Mechanics to allow resuming hostilities after a ceasefire.
        # Kept separate from vanilla types so it can be safely removed without touching any existing wargoals.

        war_name = CEASEFIRE_RESUME_WAR_NAME

        allowed = {
            always = no
        }

        available = {
        }

        take_states = {
        }

        threat = 0.5
    }
}

