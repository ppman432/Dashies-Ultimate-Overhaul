continent_declare_war_decision = {
	zebrica_declare_war_all = {
		available = {
			is_ai = no
			is_major = yes
			surrender_progress < 0.9
			date > 1017.01.01
			OR = {
				custom_override_tooltip = {
					tooltip = {
					  localization_key = zebra_continent_declare_tt
					}
					is_on_zebra_continent = yes
				}
				num_of_naval_factories > 30
			}
		}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			custom_effect_tooltip = zeb_declare_tt
			hidden_effect = {
				every_country = {
					limit = {
						is_on_zebra_continent = yes
						NOT = {
							is_in_faction_with = PREV
						}
					}
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}
	}
	griffonia_declare_war_all = {
		available = {
			is_ai = no
			is_major = yes
			surrender_progress < 0.9
			date > 1017.01.01
			OR = {
				custom_override_tooltip = {
					tooltip = {
					  localization_key = griffon_continent_declare_tt
					}
					is_on_griffon_continent = yes
				}
				num_of_naval_factories > 30
			}
		}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			custom_effect_tooltip = grf_declare_tt
			hidden_effect = {
				every_country = {
					limit = {
						is_on_griffon_continent = yes
						NOT = {
							is_in_faction_with = PREV
						}
					}
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}
	}
	equestria_declare_war_all = {
		available = {
			is_ai = no
			is_major = yes
			surrender_progress < 0.9
			date > 1017.01.01
			OR = {
				custom_override_tooltip = {
					tooltip = {
					  localization_key = pony_continent_declare_tt
					}
					is_on_equestrian_continent = yes
				}
				num_of_naval_factories > 30
			}
		}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			custom_effect_tooltip = pon_declare_tt
			hidden_effect = {
				every_country = {
					limit = {
						is_on_equestrian_continent = yes
						NOT = {
							is_in_faction_with = PREV
						}
					}
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}
	}
}